import React from 'react';
import { InstagramConnectionFlow } from '@/components/InstagramConnectionFlow';

export const InstagramConnectionFlowPage: React.FC = () => {
  return <InstagramConnectionFlow />;
};

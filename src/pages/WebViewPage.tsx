import React from 'react';
import { InstagramWebViewManager } from '@/components/InstagramWebViewManager';

const WebViewPage: React.FC = () => {
  return <InstagramWebViewManager />;
};

export default WebViewPage;
